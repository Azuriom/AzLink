rootProject.name = 'azlink'

['common', 'bukkit', 'bungee', 'sponge', 'velocity', 'nukkit', 'universal', 'universal-legacy'].each {
    include ":azlink-${it}"
    project(":azlink-${it}").projectDir = file(it)
}
